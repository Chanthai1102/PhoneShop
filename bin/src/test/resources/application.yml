spring:
    datasource:
      url: jdbc:h2:mem:testdb
      username: sa
      password:
    jpa:
      properties:
        hibernate:
          dialect: org.hibernate.dialect.H2Dialect
          hbm2ddl:
            auto: create
